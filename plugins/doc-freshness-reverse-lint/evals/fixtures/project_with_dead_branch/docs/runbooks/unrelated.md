# Unrelated runbook

This runbook mentions "no deploys by agents" as history and attaches no
condition to it at all.

Separately, and in its own paragraph about something else entirely: that other
rule only applies on Tuesdays.
