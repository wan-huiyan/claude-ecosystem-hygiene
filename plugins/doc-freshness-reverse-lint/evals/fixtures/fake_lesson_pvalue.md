# Synthetic lessons fixture

### 999. Don't sort by p-value in spec curves

**Pattern:** Sorting the spec curve by p-value instead of effect size inflates the apparent significance of specs with narrow CIs.

**Rule:** Never sort by p-value. Avoid the p-value sort. Use effect size ordering per Simonsohn 2020.
